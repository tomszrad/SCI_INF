#pragma once
#include <vector>
using namespace std;

class sortowanie
{
public:
		sortowanie();
		void bubble_sort();
		void insert_sort();
		void choice_sort();
		void quick_sort(int milego, int dnia);
		vector<int> wektor;
private:
};

